# SynthStack Web Frontend - Vue 3 + Quasar (Development)
FROM node:20-alpine

WORKDIR /app

# Copy package files
COPY package.json ./

# Install dependencies with npm (avoids pnpm symlink issues in Docker)
RUN npm install --legacy-peer-deps

# Copy source code
COPY . .

# Expose ports (Quasar dev server)
EXPOSE 3050

# Start development server
CMD ["npm", "run", "dev"]
