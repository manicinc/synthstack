<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <title>
    <%= subject %>
  </title>
  <style>
    /* Reset */
    body,
    p,
    h1,
    h2,
    h3 {
      margin: 0;
      padding: 0;
    }

    img {
      border: 0;
    }

    /* Base */
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
      line-height: 1.6;
      color: #333333;
      background-color: #f4f4f4;
    }

    /* Container */
    .email-container {
      max-width: 600px;
      margin: 0 auto;
      background-color: #ffffff;
    }

    /* Header - SynthStack purple branding */
    .email-header {
      background: linear-gradient(135deg, #6366F1 0%, #8B5CF6 100%);
      padding: 32px 24px;
      text-align: center;
    }

    .email-header h1 {
      color: #ffffff;
      font-size: 24px;
      font-weight: 700;
      margin: 0;
    }

    /* Content */
    .email-content {
      padding: 32px 24px;
    }

    .email-content p {
      margin-bottom: 16px;
      font-size: 16px;
      line-height: 1.5;
    }

    .email-content h2 {
      font-size: 20px;
      margin: 24px 0 12px;
      color: #1a1a1a;
    }

    /* Button - SynthStack primary color */
    .btn {
      display: inline-block;
      padding: 14px 28px;
      background: linear-gradient(135deg, #6366F1 0%, #8B5CF6 100%);
      color: #ffffff !important;
      text-decoration: none;
      border-radius: 8px;
      font-weight: 600;
      margin: 16px 0;
      transition: transform 0.2s;
    }

    .btn:hover {
      transform: translateY(-2px);
    }

    /* Footer */
    .email-footer {
      background-color: #f8f8f8;
      padding: 24px;
      text-align: center;
      font-size: 14px;
      color: #666666;
    }

    .email-footer a {
      color: #6366F1;
      text-decoration: none;
    }

    /* Utility */
    .text-center {
      text-align: center;
    }

    .text-muted {
      color: #666666;
      font-size: 14px;
    }

    .divider {
      height: 1px;
      background-color: #e0e0e0;
      margin: 24px 0;
    }
  </style>
</head>

<body>
  <table width="100%" cellpadding="0" cellspacing="0" style="background-color: #f4f4f4; padding: 20px 0;">
    <tr>
      <td align="center">
        <table class="email-container" cellpadding="0" cellspacing="0" width="600">
          <!-- Header -->
          <tr>
            <td class="email-header">
              <h1><%- headerTitle || 'SynthStack' %></h1>
            </td>
          </tr>

          <!-- Content -->
          <tr>
            <td class="email-content">
              <%- content %>
            </td>
          </tr>

          <!-- Footer -->
          <tr>
            <td class="email-footer">
              <p style="margin-bottom: 8px;">© <%= new Date().getFullYear() %> SynthStack by Manic Agency LLC. All
                  rights reserved.</p>
              <p style="margin-bottom: 8px;">
                <a href="<%= siteUrl || 'https://synthstack.app' %>">Visit Website</a> •
                <a href="<%= siteUrl || 'https://synthstack.app' %>/contact">Contact Support</a> •
                <a href="https://discord.gg/synthstack">Discord</a>
              </p>
              <p class="text-muted" style="margin-top: 16px; font-size: 12px;">
                This email was sent to <%= toEmail %>. If you didn't request this, please ignore it.
              </p>
            </td>
          </tr>
        </table>
      </td>
    </tr>
  </table>
</body>

</html>